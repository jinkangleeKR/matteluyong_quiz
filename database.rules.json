{
  "rules": {
    "activeGame": {
      ".read": true,
      ".write": "auth != null && auth.uid === 'REPLACE_WITH_ADMIN_UID'"
    },
    "answers": {
      "$gameId": {
        ".read": "auth != null && auth.uid === 'REPLACE_WITH_ADMIN_UID'",
        "$uid": {
          ".read": "auth != null && (auth.uid === $uid || auth.uid === 'REPLACE_WITH_ADMIN_UID')",
          "$questionIndex": {
            ".write": "auth != null && auth.uid === $uid && !data.exists() && newData.hasChildren(['choice', 'playerName', 'answeredAt'])",
            ".validate": "newData.child('choice').isNumber() && newData.child('choice').val() >= 0 && newData.child('choice').val() <= 3 && newData.child('playerName').isString() && newData.child('playerName').val().length <= 20 && newData.child('answeredAt').isNumber()"
          }
        }
      }
    }
  }
}
